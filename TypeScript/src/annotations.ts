const RANDOM_TIME= Math.round(Math.random()*500)+500;

let startTime=new Date();
//let endTime: any
//Permitimos cualquier tipo de datos a la variable
let endTime

setTimeout(() => {
    endTime=0
    endTime=new Date()
}, RANDOM_TIME);